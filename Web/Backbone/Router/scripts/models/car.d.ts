module RouterExample {
    export class Car extends Backbone.Model {
        constructor (id: number, name: string);
        public Id : number;
        public Name : string;
        public id: number;
        public name: string;
    }
}
